source_env_if_exists .env

if [ -n "${DBT_BIN:-}" ]; then
  PATH_add "$(dirname "$DBT_BIN")"
fi

if [ -n "${DUCKDB_CLI:-}" ]; then
  PATH_add "$(dirname "$DUCKDB_CLI")"
fi

export DBT_PROFILES_DIR="${DBT_PROFILES_DIR:-$PWD}"
